import 'package:flutter/material.dart';

class SobreoApp extends StatelessWidget {
  const SobreoApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
