import 'package:flutter/material.dart';

class HistoricoVenda extends StatelessWidget {
  const HistoricoVenda({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
